import {InjectionToken} from '@angular/core';

export const baseUrlToken = new InjectionToken('baseUrl');

// export const baseUrl = "http://localhost:8080/v1";
export const baseUrl = "https://api-raptorplan.herokuapp.com"
